[project]
name = "resume-agent"
version = "0.2.0"
description = "Resume Builder Multi-Agent System with structured generation pipeline"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "pydantic",
    "google-adk[extensions]",
    "google-genai",
    "ag-ui-adk",
    "jinja2",
    "openai",
]
