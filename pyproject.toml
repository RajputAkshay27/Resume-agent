[project]
name = "resume-workspace"
version = "2.0.0"
description = "Resume Builder CLI and desktop manager workspace"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "pydantic",
    "rich>=13.0",
    "click>=8.0",
    "httpx>=0.20",
]

[project.scripts]
resume-cli = "cli:cli"
